# task-list-api

### Layout

- `src/` application code
- `src/graphql/` schema, inputs, and resolver composition
- `src/db/` Prisma client wiring
- `prisma/schema.prisma` data model
- `tests/` Vitest tests
- `docker-compose.yml` local Postgres
