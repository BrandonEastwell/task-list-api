services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tasklist
      POSTGRES_PASSWORD: tasklist
      POSTGRES_DB: tasklist
    ports:
      - "5432:5432"
    volumes:
      - tasklist_pgdata:/var/lib/postgresql/data

volumes:
  tasklist_pgdata:
